@model DTOs.StaffTaskDto

@{
    ViewData["Title"] = "My Dashboard";
}

<h1>My Dashboard</h1>

<div class="row mt-4">
    <div class="col-md-6">
        <h3>My Cleaning Tasks</h3>
        @if (Model.MyCleaningTasks.Any())
        {
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Room</th>
                        <th>Status</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var task in Model.MyCleaningTasks)
                    {
                        <tr>
                            <td>@task.RoomNumber</td>
                            <td>
                                <span class="badge @(task.Status == "Pending" ? "bg-warning" : task.Status == "In Progress" ? "bg-primary" : "bg-success")">
                                    @task.Status
                                </span>
                            </td>
                            <td>
                                <a asp-controller="RoomCleaning" asp-action="Edit" asp-route-id="@task.Id" class="btn btn-sm btn-info">Update Status</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <div class="alert alert-info">No active cleaning tasks assigned.</div>
        }
    </div>

    <div class="col-md-6">
        <h3>My Maintenance Tickets</h3>
        @if (Model.MyMaintenanceTasks.Any())
        {
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Room</th>
                        <th>Reason</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var task in Model.MyMaintenanceTasks)
                    {
                        <tr>
                            <td>@task.RoomNumber</td>
                            <td>@task.Reason</td>
                            <td>
                                <form asp-controller="Maintenance" asp-action="Complete" method="post" class="d-inline">
                                    <input type="hidden" name="id" value="@task.Id" />
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-sm btn-success">Finish</button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <div class="alert alert-success">No active maintenance tickets assigned.</div>
        }
    </div>
</div>
